Creates a big floating button > left click = mount (if mounted follows nearest party member in range) > Right click = dismount (if dismounted follows nearest party member in range)
Mount can be changed in the interface > addons section
